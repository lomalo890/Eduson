round = округление числа round(3.1415, 2) округляется до второго знака после запятой
type = определить тип
import math = целый фрейм для математики ceil floor
7е-5 - научная запись числа, которая означает 7^10 * -5
Есть ещё комплексные числа: мнимое (число, после которого стоит j), реальное представление, комплексное сопряжение (берём модуль) a1.real, a1.imag

a = 1
float(a)
print(a) будет 1.0

# однострочный комментарий

На какой-то хрен сделана функция переноса строк, то есть вот это: а + б -- эквивалентно вот этому:

a + \
b

Либо:

(a +
b)

Используй круглые скобки, вместо обратного слеша.